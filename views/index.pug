extends layout

block content
  h1= title
  p Welcome to #{title}
  ul 
    each val in data
      li= val.blog_id + ": " + val.title
  br
  form(action='/new', method='POST')
    input(type='text', name='text', value='')
    button(type='submit')
      p Submit Post
  div 
